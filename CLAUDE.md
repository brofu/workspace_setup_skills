# Project Rules

## Git commits
- Always use `--author` when committing, with values from `git config --local user.name` and `git config --local user.email`, to override company env vars.
- Do not include `Co-Authored-By` lines in commit messages.
